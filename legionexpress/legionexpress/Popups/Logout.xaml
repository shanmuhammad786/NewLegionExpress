<?xml version="1.0" encoding="utf-8" ?>
<rg:PopupPage  xmlns:rg="http://rotorgames.com" 
               xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="legionexpress.Popups.Logout">
    <rg:PopupPage.Animation>
        <rg:ScaleAnimation 
            PositionIn="Bottom"
            PositionOut="Bottom"
            DurationIn="150"
            DurationOut="150"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </rg:PopupPage.Animation>
    <StackLayout 
        VerticalOptions="CenterAndExpand" 
        BackgroundColor="Transparent"
        HorizontalOptions="FillAndExpand" 
       >
        <Frame CornerRadius="20"  HorizontalOptions="FillAndExpand" BackgroundColor=" #000000 " HeightRequest="500" >
            <StackLayout Spacing="30">
                <BoxView HeightRequest="1" BackgroundColor="White" Margin="80,0"/>
                <Frame BackgroundColor="White" CornerRadius="5" Margin="20,0,20,0" Padding="15" HorizontalOptions="CenterAndExpand">
                    <StackLayout Orientation="Horizontal" Spacing="10" HorizontalOptions="CenterAndExpand">
                        <Frame  BackgroundColor="White" BorderColor="Black" WidthRequest="50" Padding="5">
                           <Label Text="COL"  TextColor="Black" FontSize="16" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                        </Frame> 
                        <Label Text="+" TextColor="Black" VerticalOptions="CenterAndExpand" FontAttributes="Bold" FontSize="15"/>
                        <Frame  BackgroundColor="White" BorderColor="Black" WidthRequest="50" Padding="5">
                            <Label Text="DEL"  TextColor="Black" FontSize="16" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" />
                        </Frame>
                        <Label Text="List" TextColor="Black" VerticalOptions="CenterAndExpand" FontAttributes="Bold" FontSize="15"/>
                    </StackLayout>
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding ColDelListCommand}"/>
                    </Frame.GestureRecognizers>
                </Frame>
                <Button HorizontalOptions="CenterAndExpand" Text="Collection Scan" Command="{Binding CollectionCommand}" IsVisible="true" BackgroundColor="White" TextColor="Black" CornerRadius="10" FontSize="20"/>
                <Button HorizontalOptions="CenterAndExpand" Text="Warehouse Scan" Command="{Binding WarehouseCommand}" IsVisible="true" BackgroundColor="White" TextColor="Black" CornerRadius="10" FontSize="20"/>
                <Button HorizontalOptions="CenterAndExpand" Text="Delivery Scan" Command="{Binding DeliveryScanCommand}" IsVisible="true" BackgroundColor="White" TextColor="Black" CornerRadius="10" FontSize="20"/>
                <Button HorizontalOptions="CenterAndExpand" Text="Messages" BackgroundColor="White" IsVisible="false" TextColor="Black" CornerRadius="10" FontSize="20"/>
                <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                    <Button Text="Cancel" Clicked="Cancel_Clicked" BackgroundColor="White" TextColor="Black" CornerRadius="10"/>
                    <Button Text="Logout" Clicked="Logout_Clicked"  BackgroundColor="White" TextColor="Black" CornerRadius="10"/>
                </StackLayout>
            </StackLayout>
        </Frame>
    </StackLayout>
</rg:PopupPage>